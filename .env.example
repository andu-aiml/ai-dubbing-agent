# ============================================================
# Environment Configuration for Dubbing Pipeline
# Copy this to .env and customize as needed
# ============================================================

# Device: 'cpu' or 'cuda' (GPU requires NVIDIA drivers + Docker GPU support)
DEVICE=cpu

# Whisper model size: tiny / base / small / medium / large / large-v3
# Larger = better quality + slower + more RAM/VRAM
# Recommended: 'medium' for a good quality/speed balance
WHISPER_MODEL=medium

# Coqui TTS model (leave as default unless you want a different model)
TTS_MODEL=tts_models/multilingual/multi-dataset/xtts_v2

# Service ports (change if there are conflicts)
ASR_PORT=8001
TTS_PORT=8002
WAV2LIP_PORT=8003
